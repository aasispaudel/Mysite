(elpy-enable)
;; Fixing a key binding bug in elpy

(define-key yas-minor-mode-map (kbd "C-c k") 'yas-expand)
;; Fixing a another key binding bug in edit ode
(define-key global map (kbd "C-c o") 'iedit-mode)
